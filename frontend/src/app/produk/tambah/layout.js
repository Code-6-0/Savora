// Layout khusus untuk halaman Tambah Produk
// Halaman ini sudah punya sidebar sendiri, jadi tidak perlu DashboardLayout wrapper

export default function TambahProdukLayout({ children }) {
  return <>{children}</>;
}
