export const metadata = {
  title: "Savora — Food Rescue",
  description: "Marketplace food rescue untuk UMKM kuliner lokal.",
};

export default function MarketplaceLayout({ children }) {
  return children;
}
