export const metadata = {
  title: "Savora — Riwayat & Impact",
  description: "Dashboard riwayat order dan dampak personal customer.",
};

export default function AkunLayout({ children }) {
  return children;
}
